export function installPrompt(origin: string) {
  return `Use Crafter UI as this project's design system. Read ${origin}/skill.md and save it to .agents/skills/crafter-ui/SKILL.md for future UI work. Follow its components and design tokens while respecting this project's existing tooling and conventions.`;
}
